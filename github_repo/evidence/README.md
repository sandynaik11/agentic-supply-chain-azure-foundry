# Evidence

Optional: drop 6–8 screenshots here that show the system working, e.g.:
- Orchestrator config (name, GPT-4.1, temp 1, connected agents)
- A successful end-to-end run (JSON + summary)
- The halted-workflow run
- Each worker agent's configuration
- The Code Interpreter / date.py trace

Keep it curated — a few clear screenshots beat a large dump.
Do NOT include any connection strings, API keys, or proprietary content.
